#!/usr/bin/expect -f
#!/usr/bin/env bash

set timeout 10

spawn sudo bash -x Travis/travis_uptimerobot_monitor_utility.sh --reset all

expect "Are you sure you wish to continue? (\[Y\]es or \[N\]o):"
send -- "n\r"

expect eof
