#!/usr/bin/expect -f
#!/usr/bin/env bash

set timeout 10

spawn sudo bash Travis/travis_uptimerobot_monitor_utility.sh --webhook

expect "Enter your webhook URL: "
send -- "${travisWebhookUrl}\r"

expect eof
