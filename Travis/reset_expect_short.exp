#!/usr/bin/expect -f
#!/usr/bin/env bash

set timeout 10

spawn sudo bash Travis/travis_uptimerobot_monitor_utility.sh -r all

expect "Are you sure you wish to continue? (\[Y\]es or \[N\]o):"
send -- "y\r"

expect eof
