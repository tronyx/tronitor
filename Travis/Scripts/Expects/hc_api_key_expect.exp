#!/usr/bin/expect -f
#!/usr/bin/env bash

set timeout 10

set apiKey $::env(travisHCApiKey)

spawn bash Travis/Scripts/travis_tronitor.sh -l

expect "Enter your provider: "
send -- "HealthChecks\r"

expect "Enter your API key: "
send -- "this-is-a-bad-api-key\r"

expect "Enter your API key: "
send -- "$apiKey\r"

expect eof
