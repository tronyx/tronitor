#!/usr/bin/expect -f
#!/usr/bin/env bash

set timeout 10

spawn kcov coverage Travis/Scripts/travis_tronitor.sh -d travistwo

expect "Are you sure you wish to continue? (\[Y\]es or \[N\]o):"
send -- "y\r"

expect eof
