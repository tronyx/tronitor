#!/usr/bin/expect -f
#!/usr/bin/env bash

set timeout 10

spawn sudo bash -x Travis/travis_uptimerobot_monitor_utility.sh --find

expect "Would you like to unpause the paused monitors? (\[Y\]es or \[N\]o): "
send -- "n\r"

expect eof
