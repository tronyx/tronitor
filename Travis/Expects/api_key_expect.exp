#!/usr/bin/expect -f
#!/usr/bin/env bash

set timeout 10

set apiKey $::env(travisApiKey)

spawn sudo bash Travis/travis_uptimerobot_monitor_utility.sh -l

expect "Enter your API key: "
send -- {$apiKey\r}

expect eof
